import type { ReactThreeFiber } from '@react-three/fiber'
import type { ShaderMaterial } from 'three'

// ─── Phase 2 — HeroGlassMaterial ─────────────────────────────────────────────
// Placeholder: Phase 2 will populate this with the actual shaderMaterial class.
// Declaring it here prevents TS errors when we reference it in JSX early.

// Pattern: add each custom material here as the phases progress.
// declare global {
//   namespace JSX {
//     interface IntrinsicElements {
//       heroGlassMaterialImpl: ReactThreeFiber.Node<HeroGlassMaterial, typeof HeroGlassMaterialImpl>
//     }
//   }
// }

export {}
